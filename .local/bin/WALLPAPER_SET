#!/bin/sh

if [ -n "$1" ]; then
  WALLPAPER_PATH=$1
else
  WALLPAPER_PATH="$WALLPAPER_DIR/default.jpg"
fi

wal -i $WALLPAPER_PATH -t --saturate 0.3 -o $HOME/.local/bin/APPLY_COLORS
qtile cmd-obj -o root -f reload_config
